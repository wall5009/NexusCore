# Data Components

Use `NexusComponents.item(modid, path)` to create component types with:

- Persistent codecs
- Network codecs
- Default values
- Cache encoding
- Tooltip callbacks
- Recipe-aware metadata
- Copy strategies
- Validation callbacks

Use `NbtToComponentMigration` for legacy item NBT migrations and `ComponentDebug` for pretty-print/diff diagnostics.
