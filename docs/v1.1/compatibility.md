# Compatibility Module Guide

Use `CompatModule` and `CompatModuleLoader` for optional integrations.

The loader checks optional mod presence, config enablement, and captures failed integrations without forcing hard dependencies.
