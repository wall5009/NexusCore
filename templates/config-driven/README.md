# Config Driven Mod Template

Create a `NexusConfig`, group options with `group(...)`, export docs with `ConfigSchemaExporter`, and open the generated owo editor with `NexusConfigScreens.open(config, parent)`.
