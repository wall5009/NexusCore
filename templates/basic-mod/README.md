# Basic NexusCore Mod Template

This template keeps beginner mod code in `common/` and builds every supported v2 target from one `build.gradle.kts`.

Run:

```bash
gradle buildAllTargets
gradle checkAllTargets
```

Launch generated clients or servers:

```bash
gradle smokeTestAllTargets
gradle runFabric1201Client
```
