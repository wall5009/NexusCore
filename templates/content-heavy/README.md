# Content Heavy Mod Template

Recommended setup:

- Use `NexusBlockSets` for ore/storage/decorative families.
- Use `NexusData.plan(modid)` for generated recipes, tags, loot tables, blockstates, and translations.
- Run `runNexusValidation` in CI.
- Use `/nexus doctor` in a development world before release.
