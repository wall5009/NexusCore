# Build Logic

The v2 repository keeps shared Gradle behavior in the root build for now. This directory is reserved for future precompiled convention plugins when the build grows beyond the current NexusCore Gradle plugin project.
