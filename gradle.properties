# Locked generated project.
# Dependency versions, loaders, and optional APIs are baked into build.gradle files.
# Regenerate from the website to change them.
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -Dfile.encoding=UTF-8
org.gradle.parallel=true
